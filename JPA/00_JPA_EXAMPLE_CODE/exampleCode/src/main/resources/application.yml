server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~//Desktop/github/h2/DB/jpa-example-code
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      show_sql: true
      format_sql: true
